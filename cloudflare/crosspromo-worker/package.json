{
  "name": "hartmann-crosspromo-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Hartmann Studios Dynamic Cross-Promotion Platform — Cloudflare Worker backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "simulate": "vitest run test/simulation.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260921.1",
    "@types/node": "^26.6.2",
    "typescript": "^5.5.4",
    "vitest": "^2.1.9",
    "wrangler": "^4.0.0"
  }
}
